# PRD — Refactor Frontend Streamlit ke Next.js

**Status:** Proposed

**Tanggal:** 2026-08-28

**Produk:** Network Monitoring

**Scope:** Penggantian frontend Streamlit dengan Next.js tanpa mengurangi fitur, menu, policy, atau kemampuan operasional yang sudah tersedia.

## 1. Latar Belakang

Frontend saat ini dibangun dengan Streamlit dan sudah mencakup dashboard monitoring, device inventory, alert, incident, threshold, serta System Health. Keterbatasan utama Streamlit untuk fase berikutnya adalah kontrol UX, routing, state browser, interaksi tabel/diagram kompleks, dan maintainability UI saat fitur bertambah.

Refactor ini mengganti **hanya frontend**. FastAPI tetap menjadi API dan pemilik business logic, MySQL tetap database utama, scheduler/collector/alert engine tidak dipindahkan ke Next.js.

Arsitektur target:

```mermaid
flowchart LR
  Browser --> Next[Next.js Frontend]
  Next --> API[FastAPI API]
  API --> DB[(MySQL)]
  Scheduler --> DB
  Scheduler --> Collectors[Collectors]
  Scheduler --> Alerts[Alert Engine]
  Alerts --> Telegram[Telegram]
```

## 2. Tujuan

1. Mencapai feature parity penuh dengan seluruh halaman Streamlit saat ini.
2. Menyediakan UX dashboard yang lebih cepat, konsisten, responsif, dan mudah dikembangkan.
3. Mempertahankan FastAPI sebagai satu-satunya sumber policy, authorization, data shaping, dan mutation.
4. Menghilangkan dependensi runtime Streamlit setelah cutover sukses.
5. Menjaga semua jalur operasional: monitoring, alert, incident, admin, export, dan System Health.

## 3. Non-Goal

- Tidak mengubah database schema hanya demi tampilan frontend.
- Tidak memindahkan collector, scheduler, alert engine, atau Telegram ke Next.js.
- Tidak mengganti FastAPI dengan route handler Next.js untuk logic bisnis.
- Tidak mengubah behavior threshold/alert/incident tanpa requirement backend terpisah.
- Tidak melakukan redesign visual yang mengorbankan informasi operasional penting.

## 4. Prinsip Arsitektur

### 4.1 Batas Tanggung Jawab

| Area | Pemilik |
| --- | --- |
| Authentication, role, API key, session validation | FastAPI |
| Device CRUD, import validation, alert/incident workflow | FastAPI |
| Collector, metric persistence, retention, alert evaluation | Scheduler/Backend |
| UI state, rendering, filter, pagination, chart, modal, form | Next.js |
| Data fetching/cache browser | Next.js client layer |

### 4.2 Kontrak API

- Gunakan endpoint FastAPI yang sudah ada; prioritaskan endpoint `paged`, `context`, `summary`, dan `freshness`.
- Jangan query MySQL langsung dari frontend.
- Jangan menduplikasi threshold, severity, grace period, RBAC, atau incident transition di frontend.
- Error API harus diterjemahkan menjadi pesan UI yang aman dan actionable.
- Endpoint legacy/deprecated tidak boleh dipakai untuk implementasi baru kecuali belum ada pengganti.

### 4.3 Security

- Session/auth mengikuti kontrak FastAPI yang sudah ada.
- Cookie/token tidak boleh ditulis ke localStorage bila backend sudah memakai cookie aman.
- API base URL dikonfigurasi lewat environment (`NEXT_PUBLIC_*` hanya untuk nilai aman/public).
- Tidak ada credential MikroTik, SNMP, Telegram, DB, JWT, atau internal API key di bundle browser.
- Browser hanya mengakses API yang diperlukan; CORS/trusted host FastAPI diperbarui untuk domain Next.js.

## 5. Teknologi Target

| Area | Pilihan |
| --- | --- |
| Framework | Next.js (App Router) + React + TypeScript strict |
| Styling | Tailwind CSS + design token internal |
| Komponen UI | shadcn/ui atau komponen internal berbasis Radix |
| Chart | Plotly.js/react-plotly.js untuk mempertahankan jenis chart saat ini |
| Table | TanStack Table dengan server-side pagination/filter bila data besar |
| Data fetching | TanStack Query/SWR; cache dan refetch eksplisit |
| Form | React Hook Form + Zod untuk validasi UX; FastAPI tetap validasi final |
| Test | Vitest/React Testing Library, Playwright E2E, API contract smoke |
| Container | Dockerfile Next.js standalone + Docker Compose service baru |

## 6. Information Architecture dan Menu

Menu berikut harus tersedia pada frontend baru dengan urutan yang setara secara fungsional:

1. Overview
2. Daily Summary
3. Live Monitoring
4. Devices
5. Alerts
6. Incidents
7. Thresholds
8. System Health
9. Profil / Session / Administration (sesuai role)

Sidebar harus mendukung status active route, collapse untuk layar kecil, informasi user/role, logout, dan hanya menampilkan aksi admin kepada admin.

## 7. Feature Parity per Halaman

### 7.1 Authentication dan Session

Route: `/login`, `/restore`, `/profile`, `/admin/*`

Requirement:

- Login, restore session, logout, logout-all, dan change password.
- Protected route redirect bila session tidak valid.
- Tampilkan nama, role, dan status session aktif.
- Halaman admin untuk user management, reset password, session management, dan audit log.
- Loading/error/expired-session state yang jelas.

Acceptance criteria:

- Viewer tidak melihat/menjalankan mutation admin.
- Session expired mengarahkan user ke login tanpa loop.
- Mutation admin selalu menggunakan endpoint FastAPI dan menampilkan hasil/error API.

### 7.2 Overview

Route: `/`

Requirement:

- Ringkasan internet, MikroTik, server, alert aktif, incident aktif, dan problem device.
- KPI cards, status badge, freshness, site context, dan tautan drill-down.
- Ringkasan insight/operational priority yang ada saat ini tetap muncul.
- Auto refresh yang dapat dinyalakan/dimatikan serta timestamp data terakhir.
- Semua visual chart menggunakan Plotly.

Acceptance criteria:

- Klik kartu/problem device membawa user ke filter/halaman detail relevan.
- Tidak ada polling berlebihan ketika tab browser tidak aktif.

### 7.3 Daily Summary

Route: `/daily-summary`

Requirement:

- Rekap uptime, ping, packet loss, jitter, dan availability harian.
- Filter rentang tanggal, device type, site, dan device bila tersedia.
- Tabel paginated/exportable dan chart Plotly untuk tren/komparasi.
- Gunakan daily summary/long-term API, bukan memuat raw metric tanpa batas.

Acceptance criteria:

- Data besar tetap memakai pagination/server-side filter.
- CSV memiliki kolom dan timezone yang konsisten dengan UI.

### 7.4 Live Monitoring

Route: `/live-monitoring`

Requirement umum:

- Filter device dan metric, range waktu, refresh control, dan deep link query parameter.
- Metric cards terbaru, status/freshness, trend line Plotly, snapshot/table paginated, CSV export.
- Semua chart line/bar/pie memakai Plotly, tooltip jelas, legend dapat dipilih, dan empty/error state.
- Device individual dan mode agregat sesuai capability.

Requirement device-specific:

- **VoIP aggregate:** opsi grup seluruh VoIP; metric card menunjukkan average/aggregate; chart menampilkan banyak line yang dapat dibedakan.
- **MikroTik:** resource, API status, connected clients, DHCP, storage, interface/firewall/queue dynamic metric yang diizinkan.
- **Printer:** kesehatan printer, status overall/error/paper/ink, toner black, uptime, total halaman, detail tray bila tersedia; jelaskan lokasi/status paper low dan collector stale/error.
- **NAS:** system/power/fan/volume/RAID/disk/temperature; tampilkan warning bahwa metric mungkin stale saat SNMP collector gagal.
- **Access point/switch/NVR/server/general device:** ping, packet loss, jitter, uptime/freshness sesuai data tersedia.

Acceptance criteria:

- Tidak ada pilihan device VoIP satuan pada mode agregat yang memang diminta disembunyikan.
- `unknown`/`unavailable` harus punya alasan/action bila collection status tersedia.
- Printer low paper menunjukkan tray/lokasi bila MIB mengirim detailnya.

### 7.5 Devices

Route: `/devices`

Requirement:

- Inventory table: nama, IP, tipe, site, location, status terakhir, freshness, active state.
- Search nama/IP/site/location; filter type/status/active; pagination server-side; CSV export.
- Admin: create/edit/delete device, confirmation delete, form site wajib dan location opsional.
- Admin: CSV dry-run import dengan error per row dan import hanya untuk row valid.
- Device type dari metadata backend, bukan hardcoded tanpa fallback.

Acceptance criteria:

- Site kosong/whitespace tidak dapat disubmit dari UI.
- HTTP 409 duplicate IP dan HTTP 422 validation diterjemahkan dengan jelas.
- Tabel tidak memakai scroll vertikal tak terbatas; gunakan pagination.

### 7.6 Alerts

Route: `/alerts`

Requirement:

- Daftar alert aktif dengan severity, device, site, type, nilai, age, status, dan freshness konteks.
- Filter severity/site/type/device, pagination, CSV export, dan direct link ke device/incident.
- Tampilkan policy notification/grace period yang relevan secara read-only.
- Bedakan alert device bisnis dengan `monitoring_degraded` collector.

Acceptance criteria:

- Alert tidak menampilkan credential/raw error sensitif.
- Status severity dan lifecycle konsisten dengan backend.

### 7.7 Incidents

Route: `/incidents`, `/incidents/[id]`

Requirement:

- Incident board/list dengan filter site/status/severity/device, pagination dan export.
- Detail incident: alert terkait, timeline, site/location, created/ack/resolved timestamps, escalation context.
- Admin action: acknowledge, resolve, reopen, update workflow sesuai endpoint FastAPI.
- Query parameter/deep link stabil untuk membuka incident tertentu.

Acceptance criteria:

- Viewer dapat melihat incident/timeline tetapi tidak melihat tombol mutation.
- Setelah action sukses, list/detail/cache direfresh tanpa reload halaman penuh.

### 7.8 Thresholds dan Maintenance Windows

Route: `/thresholds`

Requirement:

- List threshold runtime dan form edit admin.
- Threshold override per site/type/device sesuai capability API.
- Maintenance window list/create/delete dengan validasi tanggal/waktu/site.
- Jelaskan precedence global vs override dan dampak maintenance ke alert.

Acceptance criteria:

- Semua mutation memerlukan role admin.
- UI menghindari input nilai invalid; backend tetap final validator.

### 7.9 System Health

Route: `/system-health`

Requirement:

- Status backend/database, scheduler, operational alert, collector health, freshness, collector run performance.
- Scheduler timing: interval, heartbeat age, lag, stale state, duration, failures.
- Capacity: DB pool checked-out/capacity/overflow, queue/missed-window, lock contention, raw metric write rate.
- CSV export dan pagination untuk seluruh tabel besar.
- Link/runbook context yang aman untuk triage collector, VPN, ACL, SNMP, dan scheduler.

Acceptance criteria:

- Tidak menganggap data `unavailable` sebagai device failure tanpa status collector.
- Semua numerik menampilkan unit yang benar dan timestamp WIB konsisten.

## 8. Shared UI Requirements

### 8.1 Design System

- Dark operational theme setara aplikasi sekarang, dengan token warna untuk normal/warning/critical/unknown.
- Badge status tidak hanya mengandalkan warna; sertakan label teks/icon.
- Typography, spacing, card, dialog, empty state, skeleton, toast, tooltip, dan modal konsisten.
- Responsif minimal desktop 1280px dan tablet; mobile mendukung baca/ack dasar bila diperlukan.
- Accessibility: keyboard navigation, focus state, contrast, aria label, table semantic.

### 8.2 Data Table Standard

- Semua tabel besar memakai pagination; default 10/25/50/100 baris.
- URL query menyimpan filter/page penting agar dapat dibagikan.
- Loading skeleton, empty state, error state, retry, dan CSV export.
- Kolom waktu memakai WIB di UI; API tetap ISO timestamp.
- Hindari fetch semua record hanya untuk filter client-side.

### 8.3 State dan Refresh

- Reuse query key dan invalidation terpusat per domain: devices, metrics, alerts, incidents, thresholds, health.
- Auto refresh hanya pada halaman live/operasional dan dapat dimatikan user.
- Pause/reduce refetch saat tab hidden bila aman.
- Mutation memakai optimistic update hanya untuk state yang mudah dibatalkan; incident/threshold lebih aman refetch setelah sukses.

### 8.4 Error Handling

- HTTP 401/403: session/permission state.
- HTTP 409: conflict, contohnya IP device duplikat.
- HTTP 422: tampilkan validation detail pada field terkait.
- HTTP 5xx/network: pesan aman, request ID bila tersedia, tombol retry.
- Jangan mengekspos stack trace, token, URL internal sensitif, atau raw exception.

## 9. API Integration Matrix

| Frontend Domain | API utama |
| --- | --- |
| Auth | `/auth/login`, `/auth/restore`, `/auth/me`, `/auth/logout`, `/auth/admin/*` |
| Overview | `/dashboard/summary`, `/dashboard/overview-*`, `/dashboard/problem-devices` |
| Devices | `/devices/paged`, `/devices/options`, `/devices/meta/types`, CRUD `/devices` |
| Live/History | `/metrics/history/context`, `/metrics/history/paged`, `/metrics/latest-snapshot/paged`, `/metrics/names` |
| Daily/Long term | `/metrics/daily-summary`, `/metrics/long-term-explorer` |
| Alerts | `/alerts/active/paged` |
| Incidents | `/incidents/paged`, detail/timeline/workflow endpoint |
| Thresholds | `/thresholds`, `/thresholds/overrides`, `/thresholds/maintenance-windows` |
| System Health | `/health/*`, `/observability/summary`, `/metrics/freshness/summary`, `/system/performance-budgets` |

Jika endpoint tidak menyediakan data yang dibutuhkan UI, tambahkan endpoint/response FastAPI secara backward-compatible; jangan membuat query DB dari Next.js.

## 10. Deployment dan Cutover

### 10.1 Runtime Baru

- Tambah `frontend`/`nextjs` service Docker tanpa menghentikan Streamlit awal.
- Next.js dapat diakses internal melalui reverse proxy/HTTPS pada domain baru atau path yang disepakati.
- FastAPI CORS/trusted host memasukkan origin Next.js yang valid.
- Build memakai image standalone production; dependency dev tidak masuk image runtime.

### 10.2 Strategi Migrasi

1. Buat design system, API client, auth shell, sidebar, route guard.
2. Implement Overview dan System Health sebagai read-only smoke path.
3. Implement Devices dan metric/history dengan pagination/export.
4. Implement Live Monitoring dan seluruh visual/device-specific view.
5. Implement Alerts, Incidents, Thresholds, dan semua mutation admin.
6. Jalankan parity test page-by-page terhadap Streamlit.
7. Soft launch Next.js untuk admin/operator terpilih.
8. Cutover navigation/domain setelah acceptance complete.
9. Pertahankan Streamlit hanya sebagai fallback sementara dengan tanggal decommission yang diset saat cutover.
10. Hapus service/dependency Streamlit setelah observability, smoke, dan rollback window lulus.

### 10.3 Rollback

- Jika Next.js gagal, arahkan user kembali ke Streamlit tanpa mengubah collector/database/backend.
- Tidak ada migration database yang depend pada frontend cutover.
- Deployment Next.js dapat dirollback dengan image tag sebelumnya.

## 11. Quality dan Test Plan

| Layer | Requirement |
| --- | --- |
| Unit | Formatter, URL/query state, mapper API, form validation, role guard, status label |
| Component | Table pagination, chart empty/error, dialogs, device import dry-run, incident action visibility |
| Integration | Mock FastAPI response untuk semua menu dan error 401/403/409/422/5xx |
| E2E | Login, device create/edit/import, filter/export, alert drill-down, incident ack/resolve/reopen, threshold/maintenance mutation |
| Contract | Endpoint/payload compatibility dengan FastAPI; paged/cursor response dan timezone |
| Visual | Desktop/tablet screenshot untuk semua halaman, dark theme, table/chart overflow |
| Performance | Initial page, route transition, refetch, table pagination, Live Monitoring chart dengan dataset representatif |

Acceptance minimum sebelum cutover:

- Seluruh menu pada Section 6 tersedia.
- Seluruh fitur pada Section 7 lulus parity checklist.
- Tidak ada direct DB access atau business rule alert/authorization di frontend.
- API protected route dan role admin/viewer tervalidasi E2E.
- `npm run lint`, `npm run typecheck`, unit/component/E2E test, Docker build, dan FastAPI regression test lulus.
- Tidak ada secret pada bundle/image/log browser.

## 12. Delivery Checklist per Halaman

Sebuah halaman dianggap selesai hanya jika memiliki:

- Route dan sidebar/navigation entry.
- Auth/role guard yang sesuai.
- Typed API client dan query key.
- Loading, empty, error, retry, dan permission state.
- Filter/query parameter yang dapat dibagikan bila relevan.
- Pagination dan CSV export untuk tabel data besar.
- Format WIB, unit, status badge, freshness yang konsisten.
- Mobile/tablet layout minimum dan accessibility dasar.
- Unit/component/E2E test relevan.
- Parity sign-off dibanding halaman Streamlit lama.

## 13. Definition of Done — Migrasi Frontend

Migrasi dianggap selesai bila:

1. Next.js menyediakan seluruh menu dan feature parity pada PRD ini.
2. FastAPI tetap menjadi pemilik policy/backend; Streamlit tidak lagi diperlukan untuk workflow normal.
3. Semua dashboard, tabel, chart, filter, export, admin action, dan incident workflow teruji.
4. Auth, RBAC, CSRF/cookie/origin policy aman pada deployment production.
5. Docker Compose, reverse proxy, README, runbook, CI, observability, dan rollback procedure sudah diperbarui.
6. Operator/admin menandatangani UAT untuk semua menu utama.
7. Streamlit didecommission hanya setelah rollback window dan operational monitoring Next.js stabil.
