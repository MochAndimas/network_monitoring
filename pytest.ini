[pytest]
markers =
    unit: fast isolated tests that do not need app/database integration wiring
    integration: tests that exercise API, database, monitor, or service integration boundaries
    slow: tests that are useful in CI but expensive for the daily edit/test loop
    mysql: tests that require a migrated MySQL database
